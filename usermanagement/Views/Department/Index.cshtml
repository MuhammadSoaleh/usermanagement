@model IEnumerable<usermanagement.Models.Department>

<div class="container-fluid">
    <div>
        <a asp-action="Create" asp-controller="Department" class="btn btn-primary">
            Create Department
        </a>
    </div>

    <table id="myTable" class="display">
        <thead>
            <tr>
                <th>Company Name</th>
                <th>Department Name</th>
                
                <th>IsActive</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.companyname</td>
                    <td>@item.departmentname</td>
                    <td>@item.isactive</td>
                    
                    <td>
                        <a asp-controller="Department" asp-action="Edit" asp-route-id="@item.id" class="btn btn-primary">Edit</a>

                        <!-- Recommended: Use a POST form for delete -->
                        <form asp-controller="Department" asp-action="Delete" asp-route-id="@item.id" method="post" style="display:inline;">
                            <button type="submit" class="btn btn-danger" onclick="return conform('Are you sure you want to delete this user?');">Delete</button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>

    @section Scripts {
        <script>
            $(document).ready(function () {
                $('#myTable').DataTable();
            });
        </script>
    }
</div>
