@model IEnumerable<usermanagement.Models.company>

<div class="container-fluid">
    <div>
        <a asp-action="Create" asp-controller="Company" class="btn btn-primary">
            Create Company
        </a>
    </div>

    <table id="myTable" class="display">
        <thead>
            <tr>
                <th>Company Name</th>
                <th>Company Email</th>
                <th>Company Description</th>
                <th>Company Phone Number</th>
                <th>IsActive</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.name</td>
                    <td>@item.email</td>
                    <td>@item.description</td>
                    <td>@item.phonenumber</td>
                    <td>@item.isactive</td>
                    <td>
                        <a asp-controller="Company" asp-action="Edit" asp-route-id="@item.id" class="btn btn-primary">Edit</a>

                        <!-- Recommended: Use a POST form for delete -->
                        <form asp-controller="Company" asp-action="Delete" asp-route-id="@item.id" method="post" style="display:inline;">
                            <button type="submit" class="btn btn-danger" onclick="return conform('Are you sure you want to delete this user?');">Delete</button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>

    @section Scripts {
        <script>
            $(document).ready(function () {
                $('#myTable').DataTable();
            });
        </script>
    }
</div>
